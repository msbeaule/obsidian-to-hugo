obsidian_vault_path = "" # full path to vault, don't append a /

# list the folders in the vault that you want to pull from
obsidian_blog_folders = [""] # don't append a /

hugo_posts_path = "" # full path to posts folder in Hugo, don't append a /
hugo_drafts_path = "" # full path to drafts folder in Hugo, don't append a /